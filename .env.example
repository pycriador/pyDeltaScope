# ============================================
# DeltaScope - Configuração de Ambiente
# ============================================
# Copie este arquivo para .env e configure as variáveis conforme necessário
# cp .env.example .env

# ============================================
# SEGURANÇA
# ============================================

# Chave secreta para sessões Flask (OBRIGATÓRIO em produção)
# Gere uma chave segura com: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=dev-secret-key-change-in-production

# Chave de criptografia para senhas de banco de dados (OBRIGATÓRIO)
# Gere uma chave Fernet com: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# IMPORTANTE: Mantenha esta chave segura! Sem ela, não será possível descriptografar senhas salvas
ENCRYPTION_KEY=

# ============================================
# BANCO DE DADOS DA APLICAÇÃO
# ============================================

# Tipo de banco de dados para a aplicação DeltaScope
# Opções: sqlite (desenvolvimento) ou mariadb (produção)
DATABASE_TYPE=sqlite

# --- Configuração para SQLite (quando DATABASE_TYPE=sqlite) ---
# Caminho completo para o arquivo SQLite (opcional, padrão: instance/deltascope.db)
# SQLITE_DB_PATH=instance/deltascope.db

# --- Configuração para MariaDB/MySQL (quando DATABASE_TYPE=mariadb) ---
# Host do banco de dados
DB_HOST=localhost

# Porta do banco de dados (padrão: 3306)
DB_PORT=3306

# Usuário do banco de dados
DB_USER=root

# Senha do banco de dados
DB_PASSWORD=

# Nome do banco de dados
DB_NAME=deltascope_db

# ============================================
# API EXTERNA (Opcional)
# ============================================
# Configure estas variáveis se desejar enviar mudanças detectadas para uma API externa

# URL do endpoint da API externa para receber mudanças
# EXTERNAL_API_ENDPOINT=https://api.exemplo.com/v1/mudancas

# Token de autenticação para a API externa
# EXTERNAL_API_TOKEN=seu-token-aqui

# ============================================
# AMBIENTE FLASK (Opcional)
# ============================================
# O Flask detecta automaticamente o ambiente, mas você pode forçar:
# FLASK_ENV=development  # ou production
# FLASK_APP=run.py

# ============================================
# NOTAS IMPORTANTES
# ============================================
# 1. NUNCA commite o arquivo .env no Git (já está no .gitignore)
# 2. Em produção, use variáveis de ambiente do sistema ou um gerenciador de secrets
# 3. A ENCRYPTION_KEY é crítica - sem ela, senhas criptografadas não podem ser recuperadas
# 4. Gere SECRET_KEY e ENCRYPTION_KEY únicas para cada ambiente (dev, staging, prod)
# 5. Para SQLite em desenvolvimento, não é necessário configurar DB_HOST, DB_PORT, etc.
